{
  "name": "id-issuance",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open",
    "build": "vite build",
    "preview": "vite preview --open",
    "push": "git add . && git commit -m 'init' && git push"
  },
  "devDependencies": {
    "vite": "^5.4.1"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
